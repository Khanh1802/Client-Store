import { Typography } from "@mui/material";

const CheckoutPage = () => {
    return (
        <>
            <Typography>
                Only logged in users should be able to see less exclamation
            </Typography>
        </>
    )
}

export default CheckoutPage;